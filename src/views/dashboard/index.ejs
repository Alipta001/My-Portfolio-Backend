<%- include('../partials/header') %>
<header class="page-head"><div><p class="eyebrow">Control room</p><h1>Dashboard</h1></div></header><div class="stats"><% Object.entries(counts).forEach(([label, count]) => { %><a class="stat" href="<%= label === 'unread' ? '/admin/contacts' : '/admin/' + label %>"><strong><%= count %></strong><span><%= label %></span></a><% }) %></div>
<%- include('../partials/footer') %>
